import styled from 'styled-components';

export const DefaultStyle = styled.input`
    background-color:transparent;
    display:inline-block;
    position: relative;
    width: calc(100% - 24px);
    border: 1px solid #333;
    padding: 8px;
    font-size: 1rem;
    outline: none;

`;